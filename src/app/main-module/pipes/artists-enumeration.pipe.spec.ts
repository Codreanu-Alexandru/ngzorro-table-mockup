import { ArtistsEnumerationPipe } from './artists-enumeration.pipe';

describe('ArtistsEnumerationPipe', () => {
  it('create an instance', () => {
    const pipe = new ArtistsEnumerationPipe();
    expect(pipe).toBeTruthy();
  });
});
